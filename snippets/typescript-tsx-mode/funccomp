# -*- mode: snippet -*-
# name: React functional component
# key: funccomp
# --
import { FC } from "react";

type $1Props = {
     $2
};

export const $1: FC<$1Props> = ({ $3 }) => {

    return (
        $0
    );
}
